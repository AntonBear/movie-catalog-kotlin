<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Moviecatalogkotlin" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/dark</item>
        <item name="colorPrimary">@color/dark_faded</item>
        <item name="colorOnSurface">@color/gray_faded</item>          <!-- текст/иконки на фоне -->
        <item name="colorPrimaryVariant">@color/dark</item>      <!-- вариант акцента -->
        <item name="colorOnPrimary">@color/white</item>          <!-- текст на акценте -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenBackground">@color/backgroundColor</item>
        <item name="postSplashScreenTheme">@style/Theme.Moviecatalogkotlin</item>
    </style>

</resources>